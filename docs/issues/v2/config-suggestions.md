The IA plan proposes the Features config screen **suggest** features that have
data ("You created a Calcutta contest — enable Calcutta?") without auto-enabling
them (opt-in stays explicit). Not built.

## Scope
- Detect data signals per feature and show a non-intrusive "suggested" hint in the config screen.

## Acceptance
- Admins see suggestions for features that have backing data; enabling stays a deliberate action.

_Deferred._
